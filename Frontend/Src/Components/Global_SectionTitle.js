import React from 'react';

function Global_SectionTitle({ title }) {
    return (
        <div className="text-center my-8">
            <h2 className="text-3xl font-bold">{title}</h2>
        </div>
    );
}

export default Global_SectionTitle;
