import React from 'react';

const Global_SectionTitle = ({ title }) => {
  return (
    <div className="text-center py-8">
      <h1 className="text-h1 font-header">{title}</h1>
    </div>
  );
};

export default Global_SectionTitle;
